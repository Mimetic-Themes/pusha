{% doc %}
  A slide-out drawer built on a native <dialog>. This example mention of
  <dialog> inside a doc block must NOT be flagged (bucket M).
{% enddoc %}
<dialog id="cart-drawer" class="drawer">
  <div>{{ 'cart.title' | t }}</div>
</dialog>
