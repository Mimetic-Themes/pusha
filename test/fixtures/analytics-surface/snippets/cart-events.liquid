{%- comment -%}
  Truncated payload — a Liquid edit that left the JSON unbalanced. The runtime
  warns and skips it, so the event is silently lost.
{%- endcomment -%}
<script type="application/json" data-pusha-analytics-event>
  { "name": "cart_viewed", "data": { "cart": { "totalQuantity": } }
</script>
