// Global asset. Bucket G/H material, unrelated to tag casing — present so the
// layout's <SCRIPT SRC> reference resolves to a real file.
document.addEventListener('DOMContentLoaded', function () {
  window.legacyReady = true;
});
