# pusha audit
theme: <FIXTURE>
whitelists: on (default — pass --no-whitelist to disable)

## A. External <script src> — safe
  Safe. External <script src> tags are re-loaded by Pusha's syncHeadScripts on every nav.
  layout/theme.liquid:17: <script type="module" src="{{ 's-view-event.js' | asset_url }}"></script>
  blocks/banner.liquid:6: <script type="module" src="{{ 's-banner.js' | asset_url }}"></script>
  templates/product.liquid:3: <script type="module" src="{{ 's-variant-picker.js' | asset_url }}"></script>

## B. JSON data <script type="application/(ld+)?json"> — safe
  Safe. JSON data blocks are non-executable.
  templates/product.liquid:2: <script type="application/ld+json">{{ product | structured_data }}</script>

## C. Custom elements — safe (verify)
  Safe (verify). Custom elements with disconnectedCallback clean up automatically.
  assets/s-banner.js  [has disconnectedCallback]
  assets/s-view-event.js  [element registered from factory createViewEventElement() — lifecycle delegated to source, verify externally]

## D. Custom elements — needs cleanup
  Needs cleanup. Add disconnectedCallback OR wrap the section so registry.destroy can fire.
  (none)

## E. Procedural inline <script> — wrap by location
  Procedural inline script that must re-run on each swap. The correct fix depends on where it lives — see "Fix by location" below.
  blocks/countdown.liquid:2: <script>  [block]
  Fix by location:
    · block       → Promote to a custom element (connected/disconnectedCallback) — blocks have no per-block init registry.

## F. {% javascript %} blocks — F1 safe, F2 wrap by location
  F1 (custom element class) is already safe. F2 (procedural {% javascript %}) must re-run per swap — fix by location, see below.
  (none)

## G. DOMContentLoaded handlers — replace by location
  DOMContentLoaded does not re-fire after a swap. The replacement depends on where the handler lives — see "Fix by location" below.
  (none)

## H. Module-level state — hard, human review
  Human review. Module-level state and IIFEs hold closures that don't replay on swap. Check reachability — dead code can be deleted; live code needs refactor.
  (none)

## K. Portal-to-body custom elements — add data-pusha-cleanup
  Portal-to-body custom element. Survives a swap because connectedCallback moves it outside the swap container. Add `data-pusha-cleanup` to every render site so Pusha removes it before each nav.
  (none)

## L. Liquid persistent state — request-scoped Liquid in the layout shell
  Per-request Liquid in the layout shell (layout/theme.liquid, section groups, transitively-rendered snippets) freezes on first load. Sub-letters mirror the request-scoped taxonomy: A=URL/template, B=customer, C=cart, D=locale, E=per-page object, F=personalization, G=time, H=app-injected. Rank: auto=URL-derivable in JS, ask=user decides (full-reload boundary or section refetch), ok=already handled by Pusha or theme convention.
  layout/theme.liquid:2: [L-A auto] request.*
      <html lang="{{ request.locale.iso_code }}">
  layout/theme.liquid:16: [L-C ok] cart.* [verify: may self-heal via partial refresh]
      <div class="cart-badge" data-partials="cart-count">{{ cart.item_count }}</div>

## M. Persistent-shell stateful UI — close-on-nav opt-in
  Persistent-shell stateful UI — modals/drawers/overlays that lived outside #MainContent and were authored assuming a full reload would dismiss them. Three remediation options: (1) add `data-pusha-close-on-nav` to the root (Pusha strips `[open]` / sets `aria-expanded="false"` / removes body classes listed in `data-pusha-body-class-on-open`); (2) implement a `closeOnNav()` method on the custom element; (3) call `Pusha.onBeforeLeave(() => this.close())` manually. Cart drawers and persistent widgets simply omit the marker — opt-in is the safe default.
  Stateful elements in persistent shell (add data-pusha-close-on-nav, or implement closeOnNav()):
    snippets/drawer.liquid:5: [M dialog] <dialog> in persistent shell
        <dialog id="cart-drawer" class="drawer">

## J. Analytics surface
  Analytics surface. NOTE: Pusha reaches the pixel sandbox only through PREFIXED CUSTOM events — the customEvents bridge publishes pusha:page_viewed plus prefixed copies of the page-type payloads, and those are delivered to custom pixels and app pixels. What is fenced is publishing under STANDARD names, which the storefront API rejects, so a standard page_viewed never arrives on a swap. Delivery is not consumption: a third-party app pixel subscribed to the standard vocabulary has no mapping for a prefixed name, so reviving it still needs a companion custom pixel that forwards the event (docs/analytics-companion-pixel.md, README "Analytics & tracking"). This bucket checks the theme-serialized <script type="application/json" data-pusha-analytics-event> blocks anyway, because they are hand-written Liquid that nothing validates at runtime and keeping their shape right is what makes a supported publish path cheap to adopt later. Four kinds: coverage (a product/collection/search/cart page with no marker), conformance (unparseable JSON, a missing type attribute the browser then executes as JS, or a payload missing its required data key), placement (a marker in the persistent shell is re-read on every nav and would republish one page's payload forever), and raw-pixel (gtag/fbq/dataLayer calls installed directly in the theme — refire them manually from onAfterInit; do NOT migrate them into Customer Events, which would move a working pixel onto the unreachable channel).
  Gaps — silent data loss or corruption:
    (theme): [J-coverage] no product_viewed marker — the page exists but nothing re-publishes the event on a swap
  A missing or malformed marker never throws — the event simply stops reaching Meta/GA4/TikTok/Klaviyo.
  Validate on a published theme with live pixels; a preview environment won't show the loss.

## P. Partials — server-rendered refresh regions (informational)
  Informational — {% partial %} + @shopify/partial-rendering regions (new-Liquid's islands substrate). The inventory maps each partial to its consumers. The partial name is a load-bearing string contract (renaming a declaration breaks every consumer), and Pusha must coordinate its container swap with the theme's partials.apply() so a nav mid-refresh has defined ordering.
  'cart-count'  [!! consumed but not declared — typo or missing {% partial %}?]
      declared: —
      consumed: layout/theme.liquid:16
  'product-media'  [i no data-partials consumer — may be fetched in JS]
      declared: templates/product.liquid:1
      consumed: —
  The partial name is a string contract — renaming a declaration breaks every consumer.
  Dual-swap: Pusha's container swap and the theme's partials.apply() both mutate the DOM; a nav
  during an in-flight partial refresh has no defined ordering — coordinate in the runtime.

## X. Theme app extensions — app blocks, embeds, and the Script Tag blind spot
  Theme app extensions. Informational + advisory — an app's code cannot be mechanically transformed, so X inventories app surfaces and assigns each a swap-safety verdict routed by location. Two reports: X-surface lists every { "type": "@app" } declaration in a {% schema %}, split by container membership — a risk map that stays valid when the merchant installs something tomorrow, and it works with zero apps installed. X-placed recursively walks the blocks tree of every templates/*.json and sections/*.json plus config/settings_data.json -> current.blocks, and reports what is actually installed. Detection parses JSON and reads `type`: a text match misses the escaped `shopify:\/\/apps\/` encoding Shopify writes into some template files, and would report the flagship product template as app-free. Verdicts: at-risk (app block inside the swap container), survives (app block in a section group), survives-verify (app embed — outside the container, but references into the swapped region go stale with no repair signal), opaque (Script Tag API — invisible statically). App embeds with disabled: true are ignored.
  App wrapper: platform-generated fallback (no sections/apps.liquid or sections/_blocks.liquid)
  X-surface — where app blocks can land (@app schema declarations; valid with zero apps installed):
    (none)
  X-placed — apps actually installed:
    (none)
  Blind spot — Script Tag API: apps can inject <script> at runtime. Those tags are not in any theme file,
  so this audit cannot see them and their absence above is not evidence of absence. They initialize once
  and go silent after page one. Verify in a live session.

## Summary
  A external src refs:           3
  B JSON / ld+json data blocks:  1
  C/D custom elements:           2  (safe: 2, cleanup-candidate: 0)
  E procedural inline scripts:   1
  F {% javascript %} blocks:     0  (F1 safe: 0, F2 wrap: 0)
  G DOMContentLoaded handlers:   0
  H module-level state:          0
  K portal-to-body classes:      0  (0 render site(s) need data-pusha-cleanup)
  L Liquid persistent state:     2  (auto: 1, ask: 0, ok: 1)
  M persistent-shell stateful UI: 1  (modals: 1, body-class lockouts: 0)
  J analytics surface:           1  (gaps: 1, advisory: 0)
  P partials:                    2  (1 consumed-but-undeclared — likely a naming-contract gap)
  X theme app extensions:        0  (at-risk: 0, survives: 0, embeds survives-verify: 0; @app sites: 0)

## Suppressed by whitelists
  (none — no findings matched a whitelist in this run)

## Active whitelists
  files — Skip Pusha's own bundled files — pusha.liquid is framework config (E false positive), pusha.min.js is the runtime (G/H false positives).
    skips: pusha.liquid, pusha.min.js, pusha.esm.js
  H — Suppress H findings for canonical Pusha patterns (sectionInits/sectionDestroy registrations, the window.theme bootstrap shim, runtime config). These ARE the recommended patterns; flagging them would mean every successfully-ported theme shows H findings forever.
    drop /^window\.theme\s*=\s*window\.theme\s*\|\|/  (window.theme bootstrap shim)
    drop /^window\.theme\.sectionInits\s*=/  (sectionInits container init)
    drop /^window\.theme\.sectionDestroy\s*=/  (sectionDestroy container init)
    drop /^window\.theme\.sectionInits\[/  (individual sectionInits registration)
    drop /^window\.theme\.sectionDestroy\[/  (individual sectionDestroy registration)
    drop /^window\.theme\.config\s*=/  (Pusha runtime config)
  G — Suppress G findings (DOMContentLoaded handlers) in files that also call window.Pusha.on* hooks. These are typically the "fallback" branch of an `if (window.Pusha) { ... } else { addEventListener("DOMContentLoaded", ...) }` pattern — dead code on Pusha-loaded themes, kept defensively.
    skip a file's G findings if it also calls window.Pusha.on*
  Pass --no-whitelist to disable all of the above and see raw findings.

## Next steps
  An agent reading this audit can transform the findings without the pusha skill installed.
  Suggested order:
    1. K   (mechanical: add data-pusha-cleanup attribute to each render site listed above)
    2. F2  (procedural {% javascript %} — apply the fix for its [location] tag, see "Fix by location")
    3. E   (procedural inline <script> — same, routed by its [location] tag)
    4. G   (DOMContentLoaded — replace per its [location] tag, see "Fix by location")
    5. D   (add disconnectedCallback or wrap as a section to reach registry.destroy)
    6. H   (human review — these are the risky ones; check reachability first)

  Transformation contracts by location (every E/F2/G/H finding carries a [location] tag):
    section     — data-section-type="<handle>" on the root; window.theme.sectionInits[handle] = (root) => {…};
                  rewrite document.query* → root.query*; guard listeners with data-initialized.
    block       — promote to a custom element (connected/disconnectedCallback); the browser re-mounts it.
    template    — a custom element, or an onAfterInit((c, meta) => …) hook keyed on meta.template.
    shell       — runs once and persists across navs; use onFirstLoad / setupGlobal, never sectionInits.
    head-config — inert config (no call expressions); leave as-is, it is safe across swaps.

  A and B need no action. C is safe but verify external refs are tracked.
  Re-run with --json for structured output a tool can consume directly.

## Skill freshness
  CLI is @mimeticthemes/pusha@0.1.0.
  No installed skill files found at standard paths (.claude/skills/pusha/, .cursor/rules/pusha.md, .aider-conventions.md).
  Run `pusha skill --claude` (or --cursor / --aider) to install one.

## Where to find more
  PATTERNS.md — full transformation patterns, edge cases, decision trees
  SKILL.md    — end-to-end porter procedure (audit → delegate → diff → review)

  After `npm install github:mimetic-themes/pusha`:
    node_modules/@mimeticthemes/pusha/skill/{SKILL.md, PATTERNS.md}

  Or via this CLI:
    pusha audit --full                  (append PATTERNS.md to this report)
    pusha skill --print                 (dump both to stdout)
    pusha skill --claude                (→ .claude/skills/pusha/)
    pusha skill --cursor                (→ .cursor/rules/pusha.md)
    pusha skill --aider                 (→ .aider-conventions.md)
    add --global to install into ~/ instead of project-local
